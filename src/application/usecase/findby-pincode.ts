// import repository from "../../infrastructure/repositories/repository";

// const findByPincode = async(pin:number) => {
//     const nodalExist = await repository.findByPin(pin)
//     if(nodalExist) return {
//         message : "success",
//         nodal : nodalExist
//     }
// }
